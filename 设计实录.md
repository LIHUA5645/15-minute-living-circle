# 《15 分钟便民生活圈·智能体检与规划助手》设计实录

> 版权声明：肖沐樑  QQ：3387432690
> 完成时间：2026，09，18
> 文档版本：v1.0（初版设计实录）

---

## 0. 一句话定义

输入社区中心点坐标，系统在**真实步行路网**上算出 15 分钟等时圈，统计圈内民生设施覆盖，识别"服务盲区"，输出一份可导出、可复现的**体检报告**。

---

## 1. 产品形态与多端战略

| 阶段 | 形态 | 说明 |
|---|---|---|
| P0（本阶段） | 网页端 Web + Electron 桌面端 | 同一份前端源码，Electron 加载 Web 构建产物，electron-builder 打 `.exe`（NSIS） |
| P1 | 微信小程序端 | 复用 `src/core`，地图层换百度小程序 SDK |
| P2 | 安卓端 | Capacitor / uni-app 复用 Web 产物 |

**核心约束**：`src/core` 是零 DOM、零 BMap 依赖的纯计算层，通过"适配器"注入地图能力，供 Web / 小程序 / 安卓共用。

**Electron 三个硬收益**：① 渲染进程是 Chromium，BMap JS SDK 原样可用；② 主进程 Node 转发请求，自定义 `Referer` 头绕过浏览器端 AK 域名白名单；③ `userData` 落盘缓存，重复体检秒开、降配额。

**AK 策略**：浏览器端 AK `5aL6xaFpVea2H1Zmt5iXUDHtfxeSVBhn`（仅 BMap JS SDK，`.env` 注入不硬编码）。批量距离矩阵属 Web 服务 API，仅在 Electron/Node 代理侧启用；Web 端自动降级为并发算路，是合理的"桌面端更强"叙事。

---

## 2. 总体架构

```
d:\比赛专用\
├─ docs\                  设计实录 / API 策略 / 对比测试报告 / 部署指南
├─ src\
│  ├─ core\               ★ 分析引擎（纯 JS，零 DOM、零 BMap）
│  │   ├─ geo\            坐标与几何：BD09、Haversine、方位推算、Chaikin 平滑
│  │   ├─ isochrone\      等时圈：扇形采样→收敛→插值→等值线→路网吸附
│  │   ├─ poi\            检索编排 + 清洗（归一/去重/过滤/置信度）
│  │   ├─ blindspot\      盲区识别（候选网格+粗筛+精算+DBSCAN）
│  │   ├─ scoring\        六维评分 + 等级 + 建议
│  │   ├─ scheduler\      令牌桶限流 + 并发池 + 三级缓存 + 降级
│  │   └─ pipeline.js     体检流水线总编排
│  ├─ adapters\
│  │   ├─ bmapWeb\        BMap JS SDK：LocalSearch/WalkingRoute/Geocoder
│  │   ├─ bmapServer\     Web 服务 API：Place v2/Direction v2/RouteMatrix v1
│  │   └─ mock\           内置样例快照（离线兜底）
│  ├─ ui\                 React 组件层
│  ├─ styles\             设计系统
│  └─ main.jsx
├─ electron\
│  ├─ main.cjs            主进程：窗口 + API 代理 + 磁盘缓存
│  ├─ preload.cjs         contextBridge 安全桥
│  └─ builder.yml         electron-builder 配置
├─ public\sample-data\    样例社区快照
├─ scripts\               dev.bat / build.bat / build.sh / docker
├─ tests\                 vitest 单测（core 层）
├─ .github\workflows\ci.yml
├─ Dockerfile / docker-compose.yml
├─ .env.example           AK 脱敏模板
├─ LICENSE                MIT
└─ README.md
```

**依赖规则（单向）**：`ui → core ← adapters`。`core` 只认注入的 `GeoProvider` 接口：

```js
interface GeoProvider {
  walkingRoute(origin, destination) -> { durationSec, distanceM, polyline[] }
  routeMatrix(origins[], destinations[]) -> { durationSec, distanceM }[][]  // 可为 null → 降级
  searchPoi(center, keyword, radiusM, page) -> Poi[]
  reverseGeocode(point) -> { address, aoi, poiType }
}
```

---

## 3. 地图开放能力调用策略

| 能力 | 接口 | 用途 |
|---|---|---|
| 地图渲染 | BMap GL JS SDK | 底图/覆盖物/热力图/交互 |
| 地理编码 | Geocoder / geocoding/v3 | 地址↔坐标 |
| 逆地理编码 | Geocoder.getLocation / reverse_geocoding/v3 | 候选点用地性质判断 |
| POI 检索 | LocalSearch / place/v2/search | 民生设施采集 |
| 步行路径规划 | WalkingRoute / direction/v2/walking | 等时圈边界收敛 |
| 批量距离矩阵 | routematrix/v1/walking | POI 批量测距、盲区批量判定（降耗时核心） |
| 坐标转换 | geoconv/v1 | WGS84/GCJ02 → BD09 |

**配额预算**：方位数 快速12/标准24/精细36；每方位迭代 3/4/5。标准档 ≈ 24×4+40+60 ≈ 196 次请求。四节流：①令牌桶 4 QPS 突发 8；②并发池 Web6/Electron10；③三级缓存（内存→localStorage→磁盘 TTL 7天）；④邻域复用（<30m 复用）。

**降级矩阵**：QPS 限流→指数退避重试 3 次；POI 空→同义词二次检索；算路失败→blocked 标记+±15° 绕行探测，仍失败用欧氏×1.35 估算；AK 失效/断网→整切 `mock` 适配器并提示"离线演示模式"；样本不足 8→补方位+降分辨率并标注"置信度：中"。所有降级入 `report.warnings[]`。

---

## 4. 等时圈生成算法（核心）

**问题定义**：`Iso(T) = { P | walk_time_network(P0, P) ≤ T }`，本质为稀疏观测下的等值面重建。

**五阶段**：

1. **扇形方位采样**：`r0 = v_walk·T/k_raolu`，`v_walk=80 m/min`，`k_raolu=1.25` → `r0≈960m`。搜索区间 `[0.25r0, 2.2r0]`。
2. **单方位边界收敛**：目标 `f(r)=walk_time(P0,point(P0,θ,r))−T`，主用**割线法**、发散自动退回二分；收敛 `|Δr|<40m` 或 `|Δt|<45s`；邻域热启动。
3. **阻隔检测与侧向绕行**：`r_min` 处已 `f>0` → `blocked`，记录阻隔类型，`θ±15°` 加密找绕行通道；写入 `report.geshe[]`（路网割裂诊断，差异化亮点）。
4. **各向异性空间插值**：复用阶段二所有采样点 `(P_i,t_i)`+中心点构成 `≈4N` 散点场。栅格 2.4·r_max，分辨率 64/96/128。**各向异性 IDW**：`w_i=1/(d_i^2·(1+α·|Δθ_i|))`，`α≈0.6`，使等值线沿真实通行方向延展（命题鼓励的"创新空间插值"）。`blocked`/水体栅格置 `t=+∞` 防渗漏。
5. **等值线提取与路网吸附**：Marching Squares 提取 `t=T` 等值线，Chaikin 平滑 2 次；**把所有采样算路 polyline 合并成"采样路网子图"，等值线顶点 80m 内吸附到子图**（核心视觉加分项，边界贴合真实道路）。输出 5/10/15 三层 MultiPolygon 渐变热力圈层。

**请求数**：快速~36 / 标准~96 / 精细~180；缓存后二次体检 <3s。

---

## 5. 多源 POI 清洗策略

**六维分类**（每维挂同义词表）：医疗 / 教育 / 购物 / 养老 / 交通 / 休闲。必备三项（盲区判定）：菜市场、药店、小学。

**清洗 pipeline**：原始POI→①坐标归一(BD09)→②去重(硬去重 uid；软去重 名称编辑距离≤2 且距离<50m 合并；连锁去重 同品牌同街区≤200m 保留1)→③噪声过滤(黑名单词+类型白名单+半径容差)→④可信度打分 `0.4名+0.3型+0.2距+0.1址`，<0.45 进存疑区不参评→⑤分维归档。

**二次校验**：圈层边界附近(0.8~1.3r0) POI 用距离矩阵/并发算路取真实步行时间，重判"是否真正 15 分钟内"。

---

## 6. 服务盲区识别算法

**定义**：周边 1 公里步行可达范围内**同时缺乏**菜市场、药店、小学三类必备设施的居住性点。

**五步**：①候选网格（外扩 2km，步长 300/200/150m）；②居住性过滤（逆地理/AOI 去水体绿地工业，保留 150m 内有住宅 POI）；③粗筛（直线 1.6km 内无某类才精算，过滤 60%+）；④精算判定（批量距离矩阵取最近步行距离/时间，>1000m 或 >15min 该类未覆盖，三类皆缺→盲区点，两类→预警点）；⑤DBSCAN(eps=250m,minPts=2) 聚类成斑块，按 `面积×居住密度` 排序，质心作建议补建点位，产出"补建清单"。

**输出**：`mangquList[]` 含 `level/polygon/areaM2/quekou/zhongxin/jianyi/yujiFugaiRenkou`。

---

## 7. 评分模型与体检报告

**六维评分**：每维 `score_d = 100×(0.40C + 0.30A + 0.20D + 0.10B)`，其中 C 覆盖率/ A 可达性(t 越短越高)/ D 多样性(Shannon)/ B 均衡性(四象限标准差倒数)。总分 `total=Σw_d·score_d`，`w={医疗.20,教育.20,购物.20,养老.15,交通.15,休闲.10}`。等级 A≥85 / B70-84 / C55-69 / D<55。

**报告**：①总分仪表盘+等级+结论；②六维雷达图(ECharts)；③各类设施数量 vs 基准柱状图；④地图层(三层等时圈+设施散点+盲区红块+采样灰线)；⑤盲区清单+点击定位+补建建议；⑥诊断建议(规则模板触发)；⑦置信度与告警；⑧导出 PDF/PNG/JSON。

---

## 8. 可视化与交互

- 地图：BMap GL + 自定义 Polygon 圈层 + HeatmapOverlay + 设施 Marker 聚类。
- 图表：ECharts 雷达/柱状/仪表盘。
- 交互：地图选点(点击/拖拽/地址框)、密度档位切换、进度条、报告 Tab 切换、导出按钮。
- 设计系统：CSS 变量化深色专业风，左地图右报告双栏，顶部状态条。

---

## 9. 工程规范与 CI/CD

- 包管理：npm；构建：Vite；单测：vitest（core 层 100% 关键路径覆盖）。
- 代码规范：ESLint + Prettier，CI 强制 format check + lint + test + build。
- 容器：`Dockerfile`(Nginx 托管 Web 产物) + `docker-compose.yml`。
- 一键脚本：`scripts/dev.bat`(本地起开发)、`scripts/build.bat`(打包 Web)、`scripts/build.sh`(Linux/Mac)。
- 开源：MIT LICENSE；`.env.example` 脱敏；README 含环境配置/部署/架构图/评分说明。

---

## 10. 桌面端 Electron 打包

- `electron/main.cjs` 创建窗口加载 `dist/index.html`；`preload.cjs` 经 `contextBridge` 暴露 `api.*`（算路代理/批量矩阵/缓存读取/样例加载）。
- 主进程 Node 转发百度 Web 服务 API，自定义 `Referer` 头通过 AK 校验，结果写 `userData` 缓存。
- `electron-builder` 配 NSIS：`appId`、`productName`、`files: dist + electron + public`、`win.target=nsis`、`artifactName=生活圈体检助手-Setup.exe`。

---

## 11. 后续多端演进

- 小程序：adapter 换百度小程序地图组件，`searchPoi`/`walkingRoute` 用小程序 API，core 全复用。
- 安卓：Capacitor WebView 加载同产物 + 原生定位权限；或 uni-app 重壳。

---

## 12. 风险与对策

| 风险 | 对策 |
|---|---|
| AK 配额/限流 | 预算控制器 + 三级缓存 + 密度档位 + 离线 mock 兜底 |
| 纯 Web 端无法用批量矩阵 | Electron 代理侧启用；Web 端降级并发算路 |
| 插值跨障碍渗漏 | blocked 扇区 + 逆地理掩膜 + 路网吸附修正 |
| 评审查不到数据 | 内置样例快照 JSON，零配置跑通完整演示 |
| 坐标体系混乱 | 全链路统一 BD09，接入即 geoconv |

---

## 13. 里程碑

1. 脚手架（Vite+React+Electron 壳+CI+Docker+README）✅ 设计
2. core 几何/缓存/限流基础
3. bmapWeb 适配器 + 地图选点
4. 等时圈五阶段算法
5. POI 清洗 + 评分
6. 盲区识别
7. 报告 UI + 导出
8. Electron 代理 + 打包 exe
9. 对比测试报告 + 软著材料整理


## 14. 内网穿透：让静态部署页面直连本机 Node 服务

静态部署（GitHub Pages / Gitee Pages）只托管前端文件，登录 / 管理员 / AI 中转依赖的 /api、/airelay 并不存在（405 / 404）。
解决办法是把本机 Node 服务暴露成公网 https 地址，再让前端改指该地址。

### 14.1 原理

    本机 npm run dev (5173)
            |  cloudflared / ngrok / cpolar 穿透
            v
    https://xxxx.trycloudflare.com   （公网 https，可被任意页面访问）
            ^
            |  前端 ?fuwu= 参数或管理员设置写入 localStorage
    GitHub Pages 静态页面

前端新增 src/core/fuwuDiZhi.js：地址来源优先级为 URL 参数 ?fuwu= → localStorage；
未配置时原样返回相对路径（同域），本地开发行为不变。
所有后端请求（yonghu.js、zhenduan.js、aiDaohang.js、aiLiaoTian.js、admin.jsx）统一走 fuWuUrl()。
本机服务已开跨域：fuwuqi/yonghu-fuwu.mjs 的 kaiKuaYu() 与 vite 中间件 /api、/airelay、/bmapapi 均返回
Access-Control-Allow-Origin: *，并响应 OPTIONS 预检（204）。

### 14.2 操作步骤

1. 安装：cloudflared 已放于 D:\neiwangchuantou\cloudflared.exe（同目录 qidong.bat 为一键脚本）
2. 启动本机项目：双击 qidong-xiangmu.bat（npm run dev，端口 5173，MySQL 需同时运行）
3. 启动穿透：双击 qidong.bat（cloudflared tunnel --url http://localhost:5173）
   日志中 Your quick Tunnel has been created! 后那行 https 地址即为公网地址
4. 让线上页面指向本机：浏览器打开一次
   https://lihua5645.github.io/15-minute-living-circle/?fuwu=https://xxxx.trycloudflare.com
   或在管理员控制台「账号安全」页的「服务端地址」输入框填写（失焦保存）

### 14.3 关键坑

| 现象 | 原因 | 解决 |
|---|---|---|
| 穿透首页 403 | Vite 5.4+ 主机白名单校验，非 localhost 的 Host 被拒 | vite.config.js 中 server.allowedHosts = true |
| 页面 https 却要连 http 本机 | 浏览器禁止 https 页面请求 http 资源（混合内容） | 必须使用 https 穿透地址 |
| 请求被 CORS 拦截 | 本机服务未开跨域 | 已在服务端与三个中间件加 CORS 头与 OPTIONS 响应 |
| 重启后地址变了 | 免费穿透为随机域名 | 重新用 ?fuwu= 打开一次即可 |

### 14.4 备选方案

- cloudflared（免费、免注册、速度一般）：cloudflared tunnel --url http://localhost:5173
- cpolar / natapp / 花生壳（国内线路更稳，注册即用；付费可固定二级域名）