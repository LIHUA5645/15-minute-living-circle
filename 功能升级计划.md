# 《15 分钟生活圈体检助手》功能升级计划

> 作者：肖沐樑  QQ：3387432690
> 完成时间：2026，09，19
> 说明：本文档为大赛冲奖功能升级的施工总纲。每完成一项在「状态」处打勾并同步写入修复记录文档.md。
> 背景：百度地图参赛配额工单审批中，标有「需百度 AK」的步骤待工单通过后再实施，其余全部可在 OSM 模式下开发与验证。

---

## 总览与施工顺序

| 序号 | 功能 | 命中评分点 | 依赖百度 AK | 状态 |
|---|---|---|---|---|
| ① | 内层等时圈精确化 | 功能正确性 40%（热力图准确渲染） | 否 | ✅ 已完成（2026，09，19） |
| ② | 设施真实步行路线展示 | 功能正确性 + 交互 15% | 否 | ✅ 已完成（2026，09，19） |
| ③ | 盲区补建点位推荐 | 功能正确性 + 产品差异化 | 否 | 未开始 |
| ④ | AI 诊断叙述 | 赛道定位「AI 工具」 | 否 | 未开始 |
| ⑤ | 体检报告图片导出 | 交互 15%（可视化展示） | 否 | 未开始 |
| ⑥ | 上线公网演示地址 | 开源规范 15% + 交付要求 | **是** | 等工单 |

施工顺序：① → ② → ③ → ④ → ⑤ →（工单通过后）⑥。

---

## ① 内层等时圈精确化

**问题**：当前 300/600 秒内层热力圈因「中心点 + 外圈边界采样 + IDW 插值」在近中心区失真，
实测 300 秒圈仅 1,258 ㎡（理论约 32 万㎡），热力分层观感差。

**方案（分两种数据源处理）**：

- OSM 模式（默认演示模式）：适配器 `src/adapters/osm.js` 的本地 Dijkstra（`dengShiChang`）
  已持有全路网时间场。为 provider 新增 `shiJianChang(zhongXin, zuiDaMiao)` 接口，
  `src/core/isochrone/dengshiquan.js` 检测到该接口时直接把时间场写入插值网格，
  300/600/900 三条等值线全部从真实路网时间场提取，内层圈严格精确。
- 百度模式（工单通过后生效）：保持现有割线法边界搜索，但在 `qiuBianJie` 的割线迭代中
  把已产生的中间测量点（本就存在于 `yangBen`）全部作为 IDW 锚点——零额外请求，
  内层圈精度提升。

**涉及文件**：`src/core/isochrone/dengshiquan.js`、`src/adapters/osm.js`、`src/core/osmluwang.js`、
`src/core/pipeline.js`（provider 能力探测）。

**验收标准**：
1. 砂子塘社区 300 秒圈面积达到 2.5×10⁵ ~ 4×10⁵ ㎡ 合理区间（实测对比入报告）；
2. 三层热力嵌套关系正确（内 ⊂ 中 ⊕ 外），百度地图渲染无错位；
3. vitest 全部通过，`npm run build` 成功。

---

## ② 设施真实步行路线展示

**目标**：点击地图上任意设施圆点，沿真实街道画出「中心点 → 该设施」的步行路线，
直观呈现「路不是直线」的命题痛点。

**方案**：
- provider 的 `walkingRoute(origin, dest)` 接口已存在且返回 `polyline`（OSM 本地
  Dijkstra / 百度步行规划均支持），只差 UI 层消费。
- 瓦片底图（DiTuCanvas）：设施散点增加点击命中检测（距离阈值 8px），命中后调用
  provider.walkingRoute，把 polyline 折线绘制在等时圈层之上，起终点高亮。
- 百度底图（MapCanvas）：设施 Marker 绑定 click 事件，路线用 B.Polyline 渲染。
- 路线信息（距离/耗时）以小浮层显示，再次点击空白处取消。

**涉及文件**：`src/ui/DiTuCanvas.jsx`、`src/ui/MapCanvas.jsx`、`src/ui/App.jsx`（把 provider
实例下传给地图组件）。

**验收标准**：砂子塘社区点击任意超市/药店，2 秒内画出沿街路线并显示「X 米 / Y 分钟」；
路线与道路网肉眼贴合；无 AK 时（OSM 模式）功能完整可用。

---

## ③ 盲区补建点位推荐（体检 → 规划的闭环）

**目标**：盲区不只指出「缺什么」，还给出「建在哪」。每个盲区输出 1 个补建候选点，
把「15 分钟生活圈规划助手」的「规划」二字做实。

**方案**：
1. `src/core/blindspot/mangqu.js` 现有 DBSCAN 聚类输出盲区多边形；新增步骤：
   取盲区几何中心为初始候选点，向最近路网节点吸附（复用 `zuiJinJieDian`）；
2. 用 Dijkstra 时间场验证：候选点 15 分钟步行可达范围内的居住性 POI 数量即为
   「建成受益人口」，取盲区内时间场覆盖人口最大的路网节点作为最终推荐点；
3. 输出结构：`{ jingWei, yongHuZengLiang, daoZuiJinQueKouMi }`，随盲区清单进入报告
   与地图（推荐点用 ✚ 图钉高亮）。

**涉及文件**：`src/core/blindspot/mangqu.js`、`src/ui/BaoGao.jsx`、`src/ui/App.jsx`、
`src/ui/DiTuCanvas.jsx`、`src/ui/MapCanvas.jsx`。

**验收标准**：6 个盲区各输出 1 个推荐点；推荐点位于盲区内部且临近道路；
报告与地图同步展示「建议补建 + 受益人口」；测试报告补入该功能实测段落。

---

## ④ AI 诊断叙述（强化「AI 工具」赛道定位）

**目标**：体检完成后自动生成一段自然语言诊断结论（现状 + 最大短板 + 补建建议 +
预期改善），支持打字机动效逐字输出。

**方案**：
- 新建 `src/core/zhenduan.js`：规则引擎 + 模板库 NLG。输入 report（六维分数、盲区
  清单、补建推荐、耗时等），按「总评 → 优势维度 → 短板维度 → 盲区与人口 → 补建
  建议与预期改善」五段式生成 200~300 字叙述；分数区间映射形容词库（如 85+「充裕」、
  60~85「基本满足」、<60「明显不足」）；
- 报告面板顶部新增「AI 诊断」卡片，文字逐字浮现；
- 可选扩展位：预留 `VITE_LLM_API` 环境变量，配置后调用大模型润色（未配置时纯本地
  规则生成，演示零风险、零外部依赖）。

**涉及文件**：新建 `src/core/zhenduan.js`；改 `src/ui/App.jsx`、`src/ui/BaoGao.jsx`。

**验收标准**：体检结束 1 秒内开始输出诊断文字；同一份报告每次生成结论一致；
不配置任何外部 Key 时功能完整。

---

## ⑤ 体检报告图片导出

**目标**：一键生成报告长图（总评 + 雷达图 + 柱状图 + 盲区清单 + AI 诊断 + 落款），
可保存/分享，替代目前只有 JSON 的导出。

**方案**：
- 安装 `html2canvas` 依赖；对报告面板 DOM 整体截图导出 PNG；
- ECharts 图表先经 `getDataURL()` 转为 <img> 再截图，避免 canvas 跨帧问题；
- 导出按钮与现有「导出 JSON」并排放置；长图底部自动加项目名 + 作者落款。

**涉及文件**：`package.json`、`src/ui/App.jsx`、`src/ui/BaoGao.jsx`。

**验收标准**：导出 PNG 分辨率清晰（2x）、中文无乱码、图表完整；文件 < 2MB。

---

## ⑥ 上线公网演示地址（需百度 AK 工单通过）

**前置条件**：
1. 百度工单审批通过，QPS/配额提到位，且提在 `I9xUc3NE...` 所属账号；
2. 百度控制台为该 AK 的 Referer 白名单加入部署域名。

**方案**：
- 静态产物部署（`npm run build` 产物为纯静态 dist/）：优先用免费托管平台（Cloudflare
  Pages / Vercel / Gitee Pages），必须 HTTPS（浏览器定位要求安全上下文）；
- `.env.production` 填生产 AK；保留 AK 被风控时的报错提示面板；
- README 补「在线演示」章节：公网地址 + 本地 Docker 两条演示路径；
- 提交 Git 打 tag（v1.0.0），保证评审 clone 的代码与线上版本一致。

**验收标准**：公网 HTTPS 地址打开即用；定位、检索、算路、体检全流程可用；
README 演示章节齐全。

---

## 施工纪律（每一步都要遵守）

1. 每完成一项：更新本文档状态、追加修复记录文档.md、跑通 `npx vitest run` 与
   `npm run lint`；
2. 涉及评分口径的数据变化（如内层圈面积）同步更新真实对比测试报告.md；
3. 全部新代码文件开头加版权声明（肖沐樑 QQ3387432690 + 完成时间），变量命名用拼音；
4. 不引入重依赖（⑤ 的 html2canvas 除外），不做登录系统，不接需付费的外部服务。
